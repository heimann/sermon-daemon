# Build output
.zig-cache/
zig-out/

# DuckDB runtime libs downloaded by scripts/bootstrap-duckdb.sh
lib/libduckdb.so

# Local runtime state
*.db
*.db.wal
*.db.tmp
.env

# Test artifacts
test_io

# Editor/OS
.DS_Store

# Local worktrees
.worktrees/

# NER backend libs (bootstrapped, like libduckdb.so) + the GGUF model
lib/libpf.so*
lib/libggml*.so*
models/
